export default function SectionLabel({labelText}) {
  return (
    <h2 className="font-display font-bold text-h2 text-text-primary uppercase tracking-wide text-center">
      {labelText}
    </h2>
  );
}
